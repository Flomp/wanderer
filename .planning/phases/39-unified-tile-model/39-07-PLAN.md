---
phase: 39-unified-tile-model
plan: 07
type: execute
wave: 5
depends_on: ["39-05"]
files_modified:
  - app/lib/routes/navigation_screen.dart
  - app/lib/entities/active_navigation_entity.dart
  - app/lib/provider/router_provider.dart
  - app/lib/main.dart
  - app/lib/actions/launch_navigation.dart
  - app/lib/routes/trail_source_select_screen.dart
  - app/lib/objectbox-model.json
  - app/lib/objectbox.g.dart
autonomous: true
requirements: [D-01, D-16]

must_haves:
  truths:
    - "D-16: `NavigationScreen` has no `isOffline` parameter, no compose branch and no `ref.listen` fork — the twin of `TrailMap.offline`, deleted in lockstep (ROADMAP criterion 3)."
    - "D-01: `NavigationScreen` watches a single style-JSON provider and always composes through `rewriteStyleForProxy`, so a navigation session started without service picks up online tiles on the same screen when the radio returns."
    - "Every value that existed only to feed `NavigationScreen.isOffline` is removed with it: the `/navigate` route's `extra` record loses its `bool` slot, the `/record` route's `extra` map loses its `isOffline` key, and both resume pushes stop computing a style-selecting flag."
    - "The `cloud_off` icon in the maneuver banner becomes live — reading current connectivity from `onlineStatusProvider` instead of a value frozen at screen construction."
    - "D-16 extends to storage: `ActiveNavigationEntity.isOffline` is deleted from the schema, not retained. After Task 2 removes `router_provider.dart`'s `resume?.isOffline` read, nothing reads the column back, so persisting it would leave dead state that looks alive."
  artifacts:
    - path: "app/lib/routes/navigation_screen.dart"
      provides: "single-path style composition for the navigation/recording map"
      contains: "rewriteStyleForProxy"
  key_links:
    - from: "app/lib/routes/navigation_screen.dart"
      to: "app/lib/provider/map_style_json_provider.dart"
      via: "the one style provider, watched and listened unconditionally"
      pattern: "mapStyleJsonProvider"
    - from: "app/lib/provider/router_provider.dart"
      to: "app/lib/routes/navigation_screen.dart"
      via: "the /navigate extra record, now a 3-tuple"
      pattern: "NavigateResponse,\\s*\\n?\\s*ActiveNavigationEntity\\?"
---

<objective>
Delete `NavigationScreen.isOffline` outright — the parameter, both compose branches, both
`ref.listen` forks — and remove every upstream value that existed only to feed it (D-16, D-01).

Purpose: `navigation_screen.dart` is the exact twin of `trail_map.dart`; RESEARCH.md 2 and
39-PATTERNS.md both classify them as one fork implemented twice, and the same "pinned to whatever
mode it was born in" defect reproduces here at lines 1339-1341. Deleting the parameter here is
half of ROADMAP criterion 3. The plumbing that computed the flag — a `bool` slot in the
`/navigate` route's `extra` record, an `isOffline` key in the `/record` route's `extra` map, and
two resume-time reachability probes whose results were only ever used to pick a style path — goes
with it, because `main.dart`'s own comment states the flag "only selects the map style path".

Runs in parallel with Plan 06, which performs the identical deletion in `TrailMap`; the two plans
share no file.

Output: a `NavigationScreen` with one style path, a 3-slot navigate `extra`, and two live
connectivity reads replacing the two non-style uses of the old flag.
</objective>

<execution_context>
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/workflows/execute-plan.md
@/Users/christianbeutel/Documents/svelte/wanderer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-unified-tile-model/39-CONTEXT.md
@.planning/phases/39-unified-tile-model/39-RESEARCH.md
@.planning/phases/39-unified-tile-model/39-PATTERNS.md
@.planning/phases/39-unified-tile-model/39-05-SUMMARY.md
</context>

## Artifacts this phase produces

Produced by **this plan**:

| Symbol / path | Kind | Notes |
|---|---|---|
| `NavigationScreen.isOffline` | removed field + constructor parameter | |
| `/trail/:id/navigate` `extra` record | changed shape | `(NavigateResponse, ActiveNavigationEntity?, geo.Position?)` — the `bool` slot is gone |
| `/record` `extra` map key `'isOffline'` | removed | |
| `ActiveNavigationEntity.isOffline` | **removed** field + constructor parameter | ObjectBox retires property UID `4:5220757162905877938` into `retiredPropertyUids` on the next codegen run; `lib/objectbox-model.json` already carries 41 such entries, so this is routine generated output, not a hand-written migration |

<tasks>

<task type="auto">
  <name>Task 1: Remove isOffline from NavigationScreen and from the persisted entity</name>
  <files>app/lib/routes/navigation_screen.dart, app/lib/entities/active_navigation_entity.dart, app/lib/objectbox-model.json, app/lib/objectbox.g.dart</files>
  <read_first>
    - app/lib/routes/navigation_screen.dart (lines 86-130 for the field and constructor entry; 830-850 for the `ActiveNavigationEntity(... isOffline: widget.isOffline ...)` persist call in `_persistNow`; 1105-1165 for `_composeStyle`/`_swapStyle`; 1300-1350 for the `ref.listen` fork; 1370-1400 for the `baseAsync` ternary; ~1742 for the `if (widget.isOffline) ...[ Icon(Icons.cloud_off ...) ]` in the maneuver banner)
    - app/lib/components/base/trail_map.dart (its twin — read the shape of the same deletion; Plan 06 performs it there in this same wave, so do not edit that file)
    - app/lib/entities/active_navigation_entity.dart (line 36 — `bool? isOffline;` with its terse "Nullable, trail-specific." comment, and line 85 — the `this.isOffline,` constructor parameter; BOTH are deleted)
    - app/lib/objectbox-model.json (the `retiredPropertyUids` array — 41 existing entries confirm property removal is routine here; `ActiveNavigationEntity.isOffline` is id `4:5220757162905877938`)
    - app/lib/provider/online_status_provider.dart (all 52 lines — `onlineStatusProvider` is optimistic-`true` and moves on API traffic or an explicit `refresh()`; this is what the two live reads now consult)
    - .planning/phases/39-unified-tile-model/39-RESEARCH.md section 1 (the trace of why a `widget.isOffline` flip never reaches the native map)
  </read_first>
  <action>
Delete `final bool isOffline;` and `this.isOffline = false,` from `NavigationScreen`.

Style path (the D-16 core): in `build()`, replace the
`if (widget.isOffline) { ref.listen(offlineMapStyleJsonProvider, ...); } else { ref.listen(mapStyleJsonProvider, ...); }`
fork with the single unconditional `ref.listen(mapStyleJsonProvider, (_, _) => _swapStyle());`;
replace the `baseAsync` ternary with `ref.watch(mapStyleJsonProvider)`; delete the
`if (!widget.isOffline) return baseJson;` line from `_composeStyle` so the rewrite runs for every
style; and replace `_swapStyle`'s ternary with the single `ref.read(mapStyleJsonProvider).value`.

Non-style uses:
- `_persistNow`'s `ActiveNavigationEntity(...)` construction (navigation_screen.dart ~842) drops its
  `isOffline: widget.isOffline` argument entirely. Do not replace it with a live read — the field
  itself is being deleted (see below), so a replacement value would have no destination.
- The maneuver-banner `if (widget.isOffline) ...[ SizedBox, Icon(Icons.cloud_off) ]` becomes
  `if (!ref.watch(onlineStatusProvider)) ...[ ... ]`. This is a genuine improvement rather than a
  like-for-like swap: today the icon is frozen at construction and can never appear or disappear
  mid-session. Use `ref.watch` (not `ref.read`) so it tracks. If the enclosing method has no
  `WidgetRef`/`ref` in scope, read it from the state's own `ref` — `_NavigationScreenState`
  extends `ConsumerState`, so `ref` is available on the state object.

Delete `ActiveNavigationEntity.isOffline` outright: the `bool? isOffline;` field (line 36, with its
"Nullable, trail-specific." doc comment) and the `this.isOffline,` constructor parameter (line 85).

The field only ever functioned as a parameter-passing channel laundered through a persisted column:
`main.dart`'s `_pushRecordingResume` wrote it so `router_provider.dart`'s `/record` builder could
read it back one call later in the same process. Task 2 deletes both ends. `main.dart`'s own doc
comment above `_pushNavigationResume` already states the persisted value "is deliberately not
trusted" and re-probes connectivity on every resume — so the column was never durable state.
Retaining it would leave a write with no reader.

Then regenerate the ObjectBox bindings:

    cd app && dart run build_runner build --delete-conflicting-outputs

This moves property UID `4:5220757162905877938` into `retiredPropertyUids` in
`lib/objectbox-model.json` and drops the field from `lib/objectbox.g.dart`. Both generated files are
expected to change; commit them. No hand-written migration is involved — the array already holds 41
retired UIDs from prior schema changes. An existing on-device database keeps working: ObjectBox
ignores a retired column.

Update the `build()` comment block that currently explains "Offline reads the network-free
providers so no `/map/style-sources` call is ever made" — that premise is deleted. Replace it with
the true statement: one style is composed on one path and always routed through the loopback
proxy, which resolves coverage per tile and redirects uncovered tiles upstream, so a session
started without service fills in when the radio returns without a mode flip.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/routes/navigation_screen.dart lib/entities/active_navigation_entity.dart</automated>
    <automated>cd app && grep -c 'widget.isOffline\|offlineMapStyleJsonProvider' lib/routes/navigation_screen.dart</automated>
    <automated>cd app && dart run build_runner build --delete-conflicting-outputs</automated>
    <automated>cd app && git diff --stat lib/objectbox-model.json lib/objectbox.g.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && grep -c 'widget.isOffline\|offlineMapStyleJsonProvider\|bool isOffline' lib/routes/navigation_screen.dart` returns 0 (D-16).
    - `cd app && grep -v '^\s*//' lib/routes/navigation_screen.dart | grep -c 'mapStyleJsonProvider'` returns exactly 3 — listen, watch, and the `_swapStyle` read.
    - `_composeStyle` always rewrites: `sed -n '/String? _composeStyle(String? baseJson)/,/^  }/p' app/lib/routes/navigation_screen.dart | grep -c 'return baseJson;'` returns 0.
    - The banner icon is live: `grep -F 'if (!ref.watch(onlineStatusProvider))' app/lib/routes/navigation_screen.dart` matches and appears within 5 lines of `Icons.cloud_off`.
    - The persist call no longer passes the flag: `grep -c 'isOffline' app/lib/routes/navigation_screen.dart` returns 0.
    - The entity field is gone: `grep -c 'isOffline' app/lib/entities/active_navigation_entity.dart` returns 0.
    - Codegen ran and retired the property: `git diff app/lib/objectbox-model.json` is non-empty, and `5220757162905877938` now appears inside `retiredPropertyUids` rather than inside any entity's `properties` array.
    - `grep -c 'isOffline' app/lib/objectbox.g.dart` returns 0.
  </acceptance_criteria>
  <done>`NavigationScreen` has one style source and one compose path; the banner icon reads connectivity live; the persisted `isOffline` column is gone and its UID retired.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the isOffline slot from both route entry points</name>
  <files>app/lib/provider/router_provider.dart, app/lib/actions/launch_navigation.dart, app/lib/main.dart</files>
  <read_first>
    - app/lib/provider/router_provider.dart (lines 355-390 — the `/record` builder that reads `extra['isOffline']` with a `resume?.isOffline` fallback and passes `isOffline:` to `NavigationScreen`; lines 440-465 — the `/trail/:id/navigate` builder whose `extra is! (NavigateResponse, bool, ActiveNavigationEntity?, geo.Position?)` type check and 4-slot destructure both change)
    - app/lib/actions/launch_navigation.dart (lines 236-260 — two `context.push('/trail/${trail.id}/navigate', extra: (...))` calls: `(response, false, null, seedFix)` on the success path and `(cached, true, null, seedFix)` on the cached-fallback path, plus the comment above the first one naming the tuple slots)
    - app/lib/main.dart (lines 530-570 — `_pushNavigationResume` computes `final isOffline = !await ref.read(onlineStatusProvider.notifier).refresh();` and passes it in the `extra` record; `_pushRecordingResume` assigns `row.isOffline = !await ...refresh();`. The long doc comment above `_pushNavigationResume` explains the flag exists because a stale value "sends NavigationScreen down the online style path, whose `/map/style-sources` fetch hangs offline" — a premise D-01 and D-09 both delete)
    - app/lib/routes/navigation_screen.dart (as written in Task 1 — confirms the constructor no longer accepts `isOffline`)
  </read_first>
  <action>
Change the `/trail/:id/navigate` `extra` record from
`(NavigateResponse, bool, ActiveNavigationEntity?, geo.Position?)` to
`(NavigateResponse, ActiveNavigationEntity?, geo.Position?)` at all four places that name it:
- `router_provider.dart`'s `extra is! (...)` guard and its `final (response, isOffline, resumeSession, seedPosition) = extra;` destructure — the destructure becomes
  `final (response, resumeSession, seedPosition) = extra;` and the `isOffline:` argument is
  dropped from the `NavigationScreen(...)` construction.
- `launch_navigation.dart`'s two pushes: `extra: (response, null, seedFix)` and
  `extra: (cached, null, seedFix)`. Update the slot-naming comment above the first to
  `// extra: (response, resumeSeed, seedFix) — null resumeSeed for a fresh launch.`
- `main.dart`'s `_pushNavigationResume`: `extra: (response, row, null)`.

In `router_provider.dart`'s `/record` builder, delete the `final isOffline = extra is Map ? ... : (resume?.isOffline ?? false);` local, its explanatory comment (whose stated reason — "keeps
NavigationScreen off the online `/map/style-sources` fetch when offline, so the recorder never
hangs on its loading spinner" — is deleted by D-01 plus D-09's persisted fallback), and the
`isOffline:` argument.

In `main.dart`'s `_pushNavigationResume`, keep the reachability probe but drop the local:
`await ref.read(onlineStatusProvider.notifier).refresh();` — resuming a session is still a good
moment to settle the app-wide online status (Plan 09's watchdog and the sync drain both key off
it), it simply no longer selects a style. In `_pushRecordingResume`, replace
`row.isOffline = !await ref.read(onlineStatusProvider.notifier).refresh();` with the bare
`await ref.read(onlineStatusProvider.notifier).refresh();`. Rewrite `_pushNavigationResume`'s doc
comment: the persisted flag is no longer consulted at all, because there is one style path and it
resolves from a persisted `/map/style-sources` copy when the network is unavailable, so a resumed
session can no longer hang on a loading spinner for want of that fetch.
  </action>
  <verify>
    <automated>cd app && flutter analyze lib/provider/router_provider.dart lib/actions/launch_navigation.dart lib/main.dart</automated>
    <automated>cd app && grep -c 'isOffline' lib/provider/router_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && grep -c 'isOffline' lib/provider/router_provider.dart` returns 0.
    - `cd app && grep -rn "isOffline" lib/main.dart | grep -vc '^\s*[0-9]*:\s*///'` returns 0 — no code reference to the flag survives in `main.dart` (doc-comment mentions are allowed only if they describe the removal).
    - The navigate `extra` is a 3-tuple everywhere: `cd app && grep -c 'extra: (response, row, null)\|extra: (response, null, seedFix)\|extra: (cached, null, seedFix)' lib/main.dart lib/actions/launch_navigation.dart` totals 3 across the two files.
    - `grep -F 'ActiveNavigationEntity?,' app/lib/provider/router_provider.dart` matches inside the `extra is!` guard and the guard no longer names `bool`.
    - `flutter analyze lib/provider/router_provider.dart lib/actions/launch_navigation.dart lib/main.dart` reports no issues.
  </acceptance_criteria>
  <done>Neither route entry point can supply a map mode; the navigate `extra` carries only the three values still consumed.</done>
</task>

<task type="auto">
  <name>Task 3: Stop computing the recorder's offline flag</name>
  <files>app/lib/routes/trail_source_select_screen.dart</files>
  <read_first>
    - app/lib/routes/trail_source_select_screen.dart (lines 88-126 — `_openRecorder`: the `offlineFuture` probe kicked concurrently with the GPS wait, the `final isOffline = await offlineFuture;`, and the `'isOffline': isOffline` key in the `/record` `extra` map; also the file's import block, to check whether `dart:async` is already imported for `unawaited`)
    - app/lib/provider/router_provider.dart (as written in Task 2 — the `/record` builder no longer reads the `isOffline` key)
    - app/lib/provider/online_status_provider.dart (lines 35-51 — `refresh()` no-ops while the api client is unconfigured, and its result is now only used to update the app-wide status)
  </read_first>
  <action>
Delete the `'isOffline': isOffline,` entry from the `/record` `extra` map and the
`final isOffline = await offlineFuture;` line. Convert the probe from an awaited value into a
fire-and-forget status refresh: replace the `final offlineFuture = ref.read(onlineStatusProvider.notifier).refresh().then((online) => !online);`
chain with `unawaited(ref.read(onlineStatusProvider.notifier).refresh());`, adding
`import 'dart:async';` if the file does not already have it. The probe still runs and still keeps
`onlineStatusProvider` honest before a recording session starts — it simply no longer gates
anything, so the recorder no longer waits on it under the spinner.

Replace the comment block above it. Its current text ("to decide whether `NavigationScreen`
renders from the online style or the network-free offline style path ... without this flag the
recorder opens the online map, whose `/map/style-sources` fetch never resolves offline and leaves
the screen stuck on its loading spinner") is exactly the premise D-01 and D-09 delete, and leaving
it standing would mislead the next reader. Say instead: the probe settles the app-wide online
status before the session starts; the recorder's map no longer depends on it, because there is one
style path and it resolves from the persisted `/map/style-sources` copy when the network is
unreachable.
  </action>
  <verify>
    <automated>cd app && flutter analyze</automated>
    <automated>cd app && grep -c 'isOffline' lib/routes/trail_source_select_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && grep -c 'isOffline' lib/routes/trail_source_select_screen.dart` returns 0.
    - `grep -F 'unawaited(ref.read(onlineStatusProvider.notifier).refresh())' app/lib/routes/trail_source_select_screen.dart` matches — the probe survives, the gate does not.
    - `grep -c 'never resolves offline and leaves' app/lib/routes/trail_source_select_screen.dart` returns 0 — the stale comment is gone.
    - `cd app && flutter analyze` reports no issues across the whole package.
  </acceptance_criteria>
  <done>The recorder entry point starts a session without deciding a map mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route `extra` payload -> screen construction | `go_router` `extra` is untyped at the boundary; the record's shape is validated by an `is!` guard before destructuring |
| persisted session row -> resumed screen | `ActiveNavigationEntity` values are read back after a process restart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-27 | Tampering | `/navigate` `extra` shape mismatch after the tuple change | mitigate | The `extra is! (NavigateResponse, ActiveNavigationEntity?, geo.Position?)` guard is updated in the same edit as all three construction sites, and it already falls back to `TrailDetailScreen` when the shape does not match — so a stale 4-tuple surviving a deep link or process restart degrades to the detail screen rather than crashing. |
| T-39-28 | Tampering | resumed session trusting a stale persisted flag | mitigate | Nothing reads `ActiveNavigationEntity.isOffline` back to construct a screen any more; the field becomes write-only bookkeeping sourced from live connectivity at persist time, which removes the stale-value class `main.dart`'s own comment warned about. |
| T-39-29 | Denial of Service | a resumed offline session hanging on a loading spinner | mitigate | The old flag existed to keep the screen off a `/map/style-sources` fetch that never resolves offline; D-09's persisted fallback (Plan 02) makes that fetch resolve from disk instead, so the single style path cannot hang. Removing the flag without D-09 in place would reintroduce the hang — hence this plan's wave ordering behind Plan 02. |
| T-39-30 | Information Disclosure | connectivity state shown to the user going stale | mitigate | The `cloud_off` banner indicator moves from a construction-time constant to `ref.watch(onlineStatusProvider)`, so it now reflects the session's actual current state rather than a value frozen at launch. |
| T-39-SC | Tampering | npm/pip/cargo/pub installs | accept | This plan adds zero package-manager dependencies (`pubspec.yaml` is not in `files_modified`). |
</threat_model>

<verification>
- `cd app && flutter analyze` reports no issues.
- `cd app && flutter test test/provider/navigation_provider_test.dart test/provider/navigation_stats_provider_test.dart test/provider/online_status_provider_test.dart` passes.
- `git diff --stat app/lib/objectbox-model.json app/lib/objectbox.g.dart` is empty — no schema change in this plan.
- `git diff --stat app/lib/components/base/trail_map.dart` is empty (Plan 06 owns that file in this same wave).
- No file outside `app/` is modified.
</verification>

<success_criteria>
- `NavigationScreen` exposes no `isOffline` parameter and composes one style on one path (D-16, D-01).
- No route, action or resume path computes a map-mode flag.
- The user-visible offline indicator tracks live connectivity.
</success_criteria>

<output>
Create `.planning/phases/39-unified-tile-model/39-07-SUMMARY.md` when done.
</output>
